<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.itwill.staily.mypage.mapper.BookmarkMapper">


<!-- 리스트출력 -->
<select id="bookmarkList"
		parameterType="int"
		resultType="com.itwill.staily.mypage.model.dto.Bookmark">
	select *
	from product p join bookmark b
	on b.m_no = p.m_no
	join work w
	on p.w_no = w.w_no
	where p.m_no = ?
</select>

<!-- insert -->
<insert id="insertBookmark"
		parameterType="com.itwill.staily.mypage.model.dto.Bookmark">
	insert into bookmark (bm_no, m_no,p_no)  values(squence,?,?)
</insert>

<!-- delete -->
<delete id="deleteBookmark"
		parameterType="int">
	delete from bookmark
	where bm_no =?
</delete>



</mapper>